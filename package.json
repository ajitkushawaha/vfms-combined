{
  "name": "vfms-combined",
  "version": "1.0.0",
  "description": "VFMS React App with WordPress Blog as Subdirectory",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd ../vfms-frontend && npm run build && cp -r build/* ../vfms-combined/build/",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
